//
// Created by Junio Moreira on 27/08/26.
//

#include <iostream>
#include <vector>

using namespace std;

using ll = long long;

#define FAST_IO ios_base::sync_with_stdio(false); cin.tie(NULL);

// TODO: Merge two sorted arrays into a single sorted array without re-sorting
vector<ll> merge_sorted_arrays(const vector<ll>& nums1, const vector<ll>& nums2) {
    // Implementation goes here
    return {};
}

int main() {
    FAST_IO;

    // TODO: Read input (size N and array A, size M and array B), call merge_sorted_arrays, and print merged result

    return 0;
}