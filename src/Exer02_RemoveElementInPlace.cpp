//
// Created by Junio Moreira on 27/08/26.
//

#include <iostream>
#include <vector>

using namespace std;

using ll = long long;

#define FAST_IO ios_base::sync_with_stdio(false); cin.tie(NULL);

// TODO: Remove all occurrences of val in-place preserving relative order and return remaining count K
int remove_element(vector<ll>& nums, ll val) {
    // Implementation goes here
    return 0;
}

int main() {
    FAST_IO;

    // TODO: Read input (N and target Val, followed by N elements), call remove_element, and print K followed by remaining elements

    return 0;
}